mutation CreateYoutubeChannelDetails(
  $objects: [youtube_channel_details_insert_input!]!
) {
  insert_youtube_channel_details(
    objects: $objects
    on_conflict: {
      constraint: youtube_channel_details_remote_youtube_channel_id_title_descrip
      update_columns: [
        last_fetched_at
      ]
    }
  ) {
    affected_rows
  }
}
