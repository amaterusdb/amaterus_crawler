mutation CreateYoutubeVideoDetails(
  $objects: [youtube_video_details_insert_input!]!
) {
  insert_youtube_video_details(
    objects: $objects
    on_conflict: {
      constraint: youtube_video_details_values_key
      update_columns: [
        last_fetched_at
      ]
    }
  ) {
    affected_rows
  }
}
