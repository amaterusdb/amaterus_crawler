query GetUpdatableYoutubeChannels {
  youtube_channels(
    where: {
      enabled: {
        _eq: true
      }
    }
    order_by: {
      last_fetched_at: asc_nulls_first
    }
    limit: 50
  ) {
    remote_youtube_channel_id
  }
}
